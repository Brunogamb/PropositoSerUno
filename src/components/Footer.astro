---
---

<footer>
    <div class="footer-content">
        <a href="/PropositoSerUno/redes-sociales">Redes Sociales</a>
        <a href="/PropositoSerUno/aviso-legal">Aviso Legal</a>
        <a href="/PropositoSerUno/e-mails-psu">e-mails de PSU</a>
    </div>

    <div class="footer-bottom">
        <p>&copy; {new Date().getFullYear()} Propósito Ser Uno.</p>
        <p class="dev">Desarrollado por <a href="https://brunogamb.github.io/CVDEV/" target="_blank" rel="noopener noreferrer">BrunoGamb</a>
        </p>
    </div>
</footer>

<style>
footer {
    background: var(--brand-900);
    box-shadow: var(--shadow-top);
    display: flex;
    flex-direction: column; 
    align-items: center;
    justify-content: center;
    color: var(--white);
    text-wrap: balance;
    border-bottom: 13px solid var(--brand-orange-dark);
    padding: 0 20px 20px;
}

.footer-content {
    display: flex;
    gap: 150px;
    max-width: 1100px;
    width: 100%;
    padding: 50px 0 130px;
    font-weight: 500;
    font-family: var(--open-sans-font);
}

footer a {
    font-size: 24px;
    height: fit-content;
}

.footer-bottom {
    display: flex;
    justify-content: space-between; 
    width: 100%;
    max-width: 1200px;
    font-size: 14px;
    font-family: var(--open-sans-font);
    color: rgba(var(--white-rgb), 0.6);
}

.dev a {
    font-weight: 600;
    font-size: 15px;
    border-bottom: 1px solid rgba(var(--white-rgb), 0.6);
}

@media (hover: hover) {
    .dev a:hover {
        color: var(--brand-orange);
        border-bottom-color: var(--brand-orange);
        border-bottom-width: 2px;
    }
}

@media (max-width: 768px) {
    .footer-content {
        flex-direction: column;
        gap: 40px;
        text-align: center;
        padding: 40px 20px;
    }

    .footer-bottom {
        flex-direction: column;
        text-align: center;
        gap: 10px;
    }
}
</style>