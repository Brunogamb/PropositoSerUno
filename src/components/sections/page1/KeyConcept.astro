---
---

<section class="key-concept">
    <div class="container">
        <span class="eyebrow">LA CLAVE</span>

        <div class="content">
        <h3>
            <strong>La evolución mental y el desarrollo espiritual:</strong>
        </h3>
        <p>
            La evidencia científica muestra que, cuando la evolución mental y el desarrollo espiritual se integran en la vida cotidiana, la salud puede fortalecerse, el bienestar sostenerse y la vida transformarse.
        </p>
        <p>
            Desde esta comprensión desarrollamos el programa “Conquistando Salud y Bienestar”.
        </p>
        </div>

    </div>
</section>

<style>
    .key-concept {
        background-color: var(--white);
        padding: 90px 20px 40px 20px;
        color: var(--text);
        display: flex;
        justify-content: center;
    }

    .container {
        max-width: 800px;
        width: 100%;
    }

    .eyebrow {
        display: block;
        font-family: var(--open-sans-font);
        color: var(--brand-700);
        font-weight: 600;
        font-size: 18px;
        letter-spacing: 1px;
        margin-bottom: 12px;
        text-transform: uppercase;
    }

    .content {
        display: flex;
        flex-direction: column;
        gap: 10px;
        margin-bottom: 42px;
    }

    .content p, h3 {
        font-family: var(--open-sans-font);
        font-size: 21px;
        line-height: 1.6;
        font-weight: 400;
        color: var(--text-light);
        text-indent: 20px; 
        text-align: left;
    }

    h3 {
        text-indent: 0;
    }

    .content strong {
        font-weight: 600;
        color: var(--text);
    }

    @media (max-width: 768px) {

        .key-concept { 
            padding: 60px 20px; 
        }

        .content p { 
            text-indent: 30px; 
        }
    }
</style>