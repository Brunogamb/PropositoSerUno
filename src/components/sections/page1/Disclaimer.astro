---
---

<section class="disclaimer">
    <div class="container">
        <span class="eyebrow">IMPORTANTE</span>

        <div class="content">

        <p>
            Propósito Ser Uno y su programa <em>Conquistando Salud y Bienestar</em> no
            sustituyen tratamientos médicos, psicológicos o psiquiátricos.
        </p>
        <p>
            Este espacio tiene un carácter educativo y de acompañamiento, orientado a
            <strong>complementar</strong> procesos de recuperación de la salud y a <strong>favorecer</strong> estados
            sostenidos de bienestar.
        </p>

        </div>
    </div>
</section>

<style>
    .disclaimer {
        background-color: var(--brand-orange-light);
        padding: 50px 20px 40px 20px;
        color: var(--text);
        display: flex;
        justify-content: center;
    }

    .container {
        max-width: 800px;
        width: 100%;
    }

    .eyebrow {
        display: block;
        font-family: var(--open-sans-font);
        color: var(--brand-700);
        font-weight: 700;
        font-size: 20px;
        letter-spacing: 1px;
        margin-bottom: 20px;
        text-transform: uppercase;
    }

    .content {
        display: flex;
        flex-direction: column;
        gap: 10px;
        margin-bottom: 34px;
    }

    .content p {
        font-family: var(--open-sans-font);
        font-size: 21px;
        line-height: 1.6;
        font-weight: 400;
        color: var(--text-light);
        text-indent: 20px; 
        text-align: left;
    }

    .content strong {
        font-weight: 600;
        color: var(--text);
    }

    @media (max-width: 768px) {
        .disclaimer { 
            padding: 60px 20px; 
        }

        .content p { 
            text-indent: 30px; 
        }
    }
</style>