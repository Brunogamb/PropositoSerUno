---
import { Image } from 'astro:assets';
import Logo from '../../assets/Logo.webp';
import Button from '../ui/Button.astro';
---

<section class="prefooter">
    <div class="container">
        <div class="content">
            <a href="/PropositoSerUno/"><Image src={Logo} alt="Logo" class="logo" /></a>
            <h2>Comunidad en formación, hacia una organización civil sin fines de lucro</h2>
        </div>

        <div class="actions">
            <Button variant="primary" text="Hazte Miembro" href="/PropositoSerUno/membresia" className="small-btn" />
            <Button variant="outline" text="Explorar Programa" href="/PropositoSerUno/programa" className="small-btn" />
            <Button variant="primary" text="Fundadores" href="/PropositoSerUno/fundadores" className="small-btn" />
            <Button variant="secondary" text="Ver Comunidad" href="/PropositoSerUno/comunidad" className="small-btn" />
        </div>
    </div>
</section>

<style>
    .prefooter {
        background-color: var(--bg-section);
        padding: 100px 20px 24px 20px;
        color: var(--text);
        display: flex;
        justify-content: center;
    }

    .container {
        max-width: 1100px;
        width: 100%;
    }

    .content {
        display: flex;
        flex-direction: column;
        gap: 10px;
        margin-bottom: 50px;
    }

    .content h2 {
        font-family: var(--open-sans-font);
        font-size: 30px;
        line-height: 1.6;
        font-weight: 400;
        color: var(--text-light);
        text-align: left;
    }

    .actions {
        display: flex;
        justify-content: space-between;     
        width: 100%; 
    }

    :global(.btn-base.small-btn) {
        padding-left: 32px;
        padding-right: 32px;
        padding-top: 10px;
        padding-bottom: 10px;
        font-size: 24px;
    }

    .logo {
        width: 380px;
        height: auto;
    }

    a {
        width: fit-content;
    }

    @media (max-width: 768px) {
        .prefooter { 
            padding: 60px 20px; 
        }

        .content p { 
            text-indent: 30px; 
        }

        .actions {
            justify-content: center; 
            gap: 20px;
            flex-wrap: wrap;
        }
    }
</style>